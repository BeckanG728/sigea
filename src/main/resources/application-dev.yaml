spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    com.institucion.sigea: DEBUG
    org.hibernate.SQL: DEBUG

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: method
  api-docs:
    path: /api-docs