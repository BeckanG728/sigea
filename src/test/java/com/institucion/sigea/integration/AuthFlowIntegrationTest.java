package com.institucion.sigea.integration;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

/**
 * Pruebas de integración end-to-end.
 * TODO: flujo completo login -> matrícula con 2FA -> pago -> auditoría -> reportes,
 *       levantando el contexto de Spring Boot completo (@SpringBootTest).
 */
class AuthFlowIntegrationTest {

    @Test
    @Disabled("Pendiente: definir el flujo end-to-end una vez existan los módulos de negocio")
    void placeholder() {
    }
}
